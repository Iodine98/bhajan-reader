services:
  backend:
    build:
      context: backend/
      dockerfile: Dockerfile
    expose:
      - "8080"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
